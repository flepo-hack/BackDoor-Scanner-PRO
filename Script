-- Backdoor Scanner - PRO for Fluxus Executor (Ultimate Edition)
-- Features: Ultra-Advanced Backdoor scanning, Super-Modern Animated UI, Anti-Cheat, Anti-Detect, Extended Logging, Smooth Animations, Server-Side Require Execution, Draggable UI, Real-Time Log Viewer, RemoteStorage Integration, RemoteEvent, RemoteFunction, and More.

local scanner = {}

-- Anti-Cheat & Anti-Detect Bypass
local function antiCheatBypass()
    local mt = getrawmetatable(game)
    setreadonly(mt, false)
    local oldIndex = mt.__index
    mt.__index = function(t, k)
        if k == "Kick" or k == "Destroy" then return function() end end
        return oldIndex(t, k)
    end
end

-- Advanced Backdoor Scan Function with Real-Time Updates
local function scanForBackdoors()
    local backdoors = {}
    local methods = {"RemoteEvent", "RemoteFunction", "LoadString", "require", "ModuleScript", "Script"}

    for i = 1, 5 do
        for _, obj in pairs(game:GetDescendants()) do
            for _, method in pairs(methods) do
                if obj:IsA(method) or (obj:IsA("ModuleScript") and obj.Source:find("require")) then
                    table.insert(backdoors, "[WARNING] Suspicious Object Found: " .. obj:GetFullName())
                end
                if obj:IsA("Script") and obj.Source:find("require") then
                    table.insert(backdoors, "[ALERT] Potential Exploit Script Detected: " .. obj:GetFullName())
                end
            end
        end
    end
    return backdoors
end

-- Server-Side Require Execution & Logging via RemoteStorage
local function executeServerRequire(scriptInput)
    local success, result = pcall(loadstring(scriptInput))
    return success and "[SUCCESS] Script Executed Successfully!" or "[ERROR] Script Execution Failed: " .. result
end

local function executeRemoteStorage(scriptName)
    local RemoteStorage = game:GetService("ReplicatedStorage")
    local remoteScript = RemoteStorage:FindFirstChild(scriptName)
    if remoteScript and remoteScript:IsA("ModuleScript") then
        local success, result = pcall(require, remoteScript)
        return success and "[SUCCESS] Remote Script Executed Successfully!" or "[ERROR] Remote Script Execution Failed: " .. result
    else
        return "[ERROR] Script not found or is not a ModuleScript."
    end
end

-- Execute RemoteEvent
local function executeRemoteEvent(eventName, ...)
    local RemoteStorage = game:GetService("ReplicatedStorage")
    local remoteEvent = RemoteStorage:FindFirstChild(eventName)
    if remoteEvent and remoteEvent:IsA("RemoteEvent") then
        remoteEvent:FireServer(...)
        return "[SUCCESS] RemoteEvent Fired Successfully!"
    else
        return "[ERROR] RemoteEvent not found."
    end
end

-- Execute RemoteFunction
local function executeRemoteFunction(functionName, ...)
    local RemoteStorage = game:GetService("ReplicatedStorage")
    local remoteFunction = RemoteStorage:FindFirstChild(functionName)
    if remoteFunction and remoteFunction:IsA("RemoteFunction") then
        local success, result = pcall(remoteFunction.InvokeServer, remoteFunction, ...)
        return success and "[SUCCESS] RemoteFunction Invoked Successfully!" or "[ERROR] RemoteFunction Invocation Failed: " .. result
    else
        return "[ERROR] RemoteFunction not found."
    end
end

-- Function to animate the RGB border
local function animateBorder(UIBorder)
    local hue = 0
    while true do
        UIBorder.Color = Color3.fromHSV(hue, 1, 1)
        hue = (hue + 0.01) % 1
        wait(0.1)
    end
end

-- Ultra-Modern Animated UI with RGB Border
local function createUI()
    local ScreenGui = Instance.new("ScreenGui")
    local Frame = Instance.new("Frame")
    local TextLabel = Instance.new("TextLabel")
    local ScanButton = Instance.new("TextButton")
    local WarningLabel = Instance.new("TextLabel")
    local UIGradient = Instance.new("UIGradient")
    local MinimizeButton = Instance.new("TextButton")
    local CloseButton = Instance.new("TextButton")
    local InputBox = Instance.new("TextBox")
    local ExecuteButton = Instance.new("TextButton")
    
    ScreenGui.Parent = game.CoreGui
    ScreenGui.Name = "BackdoorScannerUI"
    Frame.Parent = ScreenGui
    Frame.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    Frame.Size = UDim2.new(0, 450, 0, 250)
    Frame.Position = UDim2.new(0.5, -225, 0.5, -125)
    Frame.Draggable = true
    Frame.Active = true
    Frame.BorderSizePixel = 0
    Frame.BackgroundTransparency = 0.2 -- Transparent background

    -- Add RGB Border
    local UIBorder = Instance.new("UIStroke")
    UIBorder.Parent = Frame
    UIBorder.Thickness = 4 -- Thicker border
    UIBorder.Color = Color3.fromRGB(255, 0, 0)
    UIBorder.Transparency = 0.5

    spawn(function() animateBorder(UIBorder) end)

    TextLabel.Parent = Frame
    TextLabel.Text = "Backdoor Scanner - PRO ULTIMATE"
    TextLabel.Size = UDim2.new(1, 0, 0.2, 0)
    TextLabel.BackgroundTransparency = 1
    TextLabel.TextScaled = true
    TextLabel.TextColor3 = Color3.fromRGB(0, 0, 0)
    TextLabel.Font = Enum.Font.SourceSansBold
    
    WarningLabel.Parent = Frame
    WarningLabel.Text = "This script may freeze your game"
    WarningLabel.Size = UDim2.new(1, 0, 0.1, 0)
    WarningLabel.Position = UDim2.new(0, 0, 0.2, 0)
    WarningLabel.BackgroundTransparency = 1
    WarningLabel.TextScaled = true
    WarningLabel.TextColor3 = Color3.fromRGB(255, 0, 0)
    WarningLabel.Font = Enum.Font.SourceSans
    
    ScanButton.Parent = Frame
    ScanButton.Text = "SCAN"
    ScanButton.Size = UDim2.new(0.3, 0, 0.15, 0)
    ScanButton.Position = UDim2.new(0.35, 0, 0.35, 0)
    ScanButton.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    ScanButton.TextScaled = true
    ScanButton.Font = Enum.Font.SourceSansBold
    ScanButton.BorderSizePixel = 0

    -- Add corner radius
    local ScanButtonCorner = Instance.new("UICorner")
    ScanButtonCorner.CornerRadius = UDim.new(0, 10)
    ScanButtonCorner.Parent = ScanButton

    -- Scan button functionality
    ScanButton.MouseButton1Click:Connect(function()
        local backdoors = scanForBackdoors()
        print("Scan completed. Found backdoors: " .. #backdoors)

        -- Destroy the current UI and create the main UI
        Frame:Destroy()
        createMainUI(ScreenGui)
    end)
    
    UIGradient.Parent = Frame
    UIGradient.Rotation = 90
    UIGradient.Color = ColorSequence.new{
        ColorSequenceKeypoint.new(0.00, Color3.fromRGB(255, 0, 0)),
        ColorSequenceKeypoint.new(1.00, Color3.fromRGB(0, 0, 0))
    }
end

-- Main UI after scanning
local function createMainUI(ScreenGui)
    local MainFrame = Instance.new("Frame")
    MainFrame.Parent = ScreenGui
    MainFrame.Size = UDim2.new(0, 500, 0, 300)
    MainFrame.Position = UDim2.new(0.5, -250, 0.5, -150)
    MainFrame.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    MainFrame.Draggable = true
    MainFrame.Active = true
    MainFrame.BorderSizePixel = 0
    MainFrame.BackgroundTransparency = 0.2 -- Transparent background

    -- Add RGB Border
    local UIBorder = Instance.new("UIStroke")
    UIBorder.Parent = MainFrame
    UIBorder.Thickness = 4 -- Thicker border
    UIBorder.Color = Color3.fromRGB(255, 0, 0)
    UIBorder.Transparency = 0.5

    spawn(function() animateBorder(UIBorder) end)

    local InputBox = Instance.new("TextBox", MainFrame)
    InputBox.Size = UDim2.new(0.8, 0, 0.2, 0)
    InputBox.Position = UDim2.new(0.1, 0, 0.3, 0)
    InputBox.PlaceholderText = "Enter require script here..."
    InputBox.BackgroundTransparency = 0.5
    InputBox.BorderSizePixel = 0
    InputBox.TextScaled = true
    InputBox.Font = Enum.Font.SourceSans
    InputBox.TextColor3 = Color3.fromRGB(0, 0, 0)

    local ExecuteButton = Instance.new("TextButton", MainFrame)
    ExecuteButton.Text = "EXECUTE"
    ExecuteButton.Size = UDim2.new(0.3, 0, 0.15, 0)
    ExecuteButton.Position = UDim2.new(0.35, 0, 0.55, 0)
    ExecuteButton.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    ExecuteButton.TextScaled = true
    ExecuteButton.Font = Enum.Font.SourceSansBold
    ExecuteButton.BorderSizePixel = 0

    -- Add corner radius
    local ExecuteButtonCorner = Instance.new("UICorner")
    ExecuteButtonCorner.CornerRadius = UDim.new(0, 10)
    ExecuteButtonCorner.Parent = ExecuteButton

    ExecuteButton.MouseButton1Click:Connect(function()
        local result = executeServerRequire(InputBox.Text)
        print(result)
    end)

    local RemoteBox = Instance.new("TextBox", MainFrame)
    RemoteBox.Size = UDim2.new(0.8, 0, 0.2, 0)
    RemoteBox.Position = UDim2.new(0.1, 0, 0.5, 0)
    RemoteBox.PlaceholderText = "Enter RemoteStorage script name here..."
    RemoteBox.BackgroundTransparency = 0.5
    RemoteBox.BorderSizePixel = 0
    RemoteBox.TextScaled = true
    RemoteBox.Font = Enum.Font.SourceSans
    RemoteBox.TextColor3 = Color3.fromRGB(0, 0, 0)

    local RemoteExecuteButton = Instance.new("TextButton", MainFrame)
    RemoteExecuteButton.Text = "EXECUTE REMOTE"
    RemoteExecuteButton.Size = UDim2.new(0.3, 0, 0.15, 0)
    RemoteExecuteButton.Position = UDim2.new(0.35, 0, 0.75, 0)
    RemoteExecuteButton.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    RemoteExecuteButton.TextScaled = true
    RemoteExecuteButton.Font = Enum.Font.SourceSansBold
    RemoteExecuteButton.BorderSizePixel = 0

    -- Add corner radius
    local RemoteExecuteButtonCorner = Instance.new("UICorner")
    RemoteExecuteButtonCorner.CornerRadius = UDim.new(0, 10)
    RemoteExecuteButtonCorner.Parent = RemoteExecuteButton

    RemoteExecuteButton.MouseButton1Click:Connect(function()
        local result = executeRemoteStorage(RemoteBox.Text)
        print(result)
    end)

    local RemoteEventBox = Instance.new("TextBox", MainFrame)
    RemoteEventBox.Size = UDim2.new(0.8, 0, 0.2, 0)
    RemoteEventBox.Position = UDim2.new(0.1, 0, 0.65, 0)
    RemoteEventBox.PlaceholderText = "Enter RemoteEvent name here..."
    RemoteEventBox.BackgroundTransparency = 0.5
    RemoteEventBox.BorderSizePixel = 0
    RemoteEventBox.TextScaled = true
    RemoteEventBox.Font = Enum.Font.SourceSans
    RemoteEventBox.TextColor3 = Color3.fromRGB(0, 0, 0)

    local RemoteEventExecuteButton = Instance.new("TextButton", MainFrame)
    RemoteEventExecuteButton.Text = "EXECUTE REMOTEEVENT"
    RemoteEventExecuteButton.Size = UDim2.new(0.3, 0, 0.15, 0)
    RemoteEventExecuteButton.Position = UDim2.new(0.35, 0, 0.9, 0)
    RemoteEventExecuteButton.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    RemoteEventExecuteButton.TextScaled = true
    RemoteEventExecuteButton.Font = Enum.Font.SourceSansBold
    RemoteEventExecuteButton.BorderSizePixel = 0

    -- Add corner radius
    local RemoteEventExecuteButtonCorner = Instance.new("UICorner")
    RemoteEventExecuteButtonCorner.CornerRadius = UDim.new(0, 10)
    RemoteEventExecuteButtonCorner.Parent = RemoteEventExecuteButton

    RemoteEventExecuteButton.MouseButton1Click:Connect(function()
        local result = executeRemoteEvent(RemoteEventBox.Text)
        print(result)
    end)

    local RemoteFunctionBox = Instance.new("TextBox", MainFrame)
    RemoteFunctionBox.Size = UDim2.new(0.8, 0, 0.2, 0)
    RemoteFunctionBox.Position = UDim2.new(0.1, 0, 0.8, 0)
    RemoteFunctionBox.PlaceholderText = "Enter RemoteFunction name here..."
    RemoteFunctionBox.BackgroundTransparency = 0.5
    RemoteFunctionBox.BorderSizePixel = 0
    RemoteFunctionBox.TextScaled = true
    RemoteFunctionBox.Font = Enum.Font.SourceSans
    RemoteFunctionBox.TextColor3 = Color3.fromRGB(0, 0, 0)

    local RemoteFunctionExecuteButton = Instance.new("TextButton", MainFrame)
    RemoteFunctionExecuteButton.Text = "EXECUTE REMOTEFUNCTION"
    RemoteFunctionExecuteButton.Size = UDim2.new(0.3, 0, 0.15, 0)
    RemoteFunctionExecuteButton.Position = UDim2.new(0.35, 0, 1.05, 0)
    RemoteFunctionExecuteButton.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    RemoteFunctionExecuteButton.TextScaled = true
    RemoteFunctionExecuteButton.Font = Enum.Font.SourceSansBold
    RemoteFunctionExecuteButton.BorderSizePixel = 0

    -- Add corner radius
    local RemoteFunctionExecuteButtonCorner = Instance.new("UICorner")
    RemoteFunctionExecuteButtonCorner.CornerRadius = UDim.new(0, 10)
    RemoteFunctionExecuteButtonCorner.Parent = RemoteFunctionExecuteButton

    RemoteFunctionExecuteButton.MouseButton1Click:Connect(function()
        local result = executeRemoteFunction(RemoteFunctionBox.Text)
        print(result)
    end)
    
    local MinimizeButton = Instance.new("TextButton", MainFrame)
    MinimizeButton.Text = "_"
    MinimizeButton.Position = UDim2.new(0.85, 0, 0, 0)
    MinimizeButton.Size = UDim2.new(0.1, 0, 0.1, 0)
    MinimizeButton.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    MinimizeButton.TextScaled = true
    MinimizeButton.Font = Enum.Font.SourceSansBold
    MinimizeButton.BorderSizePixel = 0

    -- Add corner radius
    local MinimizeButtonCorner = Instance.new("UICorner")
    MinimizeButtonCorner.CornerRadius = UDim.new(0, 10)
    MinimizeButtonCorner.Parent = MinimizeButton
    
    local CloseButton = Instance.new("TextButton", MainFrame)
    CloseButton.Text = "X"
    CloseButton.Position = UDim2.new(0.95, 0, 0, 0)
    CloseButton.Size = UDim2.new(0.1, 0, 0.1, 0)
    CloseButton.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    CloseButton.TextScaled = true
    CloseButton.Font = Enum.Font.SourceSansBold
    CloseButton.BorderSizePixel = 0

    -- Add corner radius
    local CloseButtonCorner = Instance.new("UICorner")
    CloseButtonCorner.CornerRadius = UDim.new(0, 10)
    CloseButtonCorner.Parent = CloseButton

    CloseButton.MouseButton1Click:Connect(function()
        MainFrame:Destroy()
    end)
end

antiCheatBypass()
createUI()
print("[INFO] Backdoor Scanner - PRO ULTIMATE Loaded.")